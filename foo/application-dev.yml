spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/debug-live/config-demo.git
          search-paths: foo
server:
  port: 8002
